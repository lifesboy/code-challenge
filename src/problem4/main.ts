function sum_to_n_a(n: number): number {
    return n
}

function sum_to_n_b(n: number): number {
    return n
}

function sum_to_n_c(n: number): number {
    return n
}

console.log('Start')

console.assert(sum_to_n_a(0) === 0, 'sum_to_n_a:0 => FAIL')


console.log('Done')